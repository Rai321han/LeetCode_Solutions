#include <iostream>
#include <vector>
#include <string>

using namespace std;

string addBinary(string a, string b) {
    int size_a = a.size();
    int size_b = b.size();
    int i = size_a; j = size_b;
    while(i>=0 || j>=0; )
}

int main() {
    string a = "10110";
    string b = "1001";

    addBinary(a,b);
    return 0;
}